apiVersion: 1

providers:
  - name: task-reloader-dashboards
    orgId: 1
    folder: Task Reloader
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/dashboards

# 반복주기 문구 개선

## 작업 내용

- 작업 목록 카드의 반복 주기 문구를 `N일마다 반복`으로 변경했다.
- 작업 상세 화면의 반복 주기 문구도 `N일마다 반복`으로 통일했다.

## 테스트 방법

- 작업 목록에서 반복 주기가 `7일마다 반복`처럼 표시되는지 확인한다.
- 작업 상세 화면에서 반복 주기가 같은 문구로 표시되는지 확인한다.

## 관련 테스트

- `npm run type-check`
